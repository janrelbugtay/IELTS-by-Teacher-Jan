const fs = require('fs');
// Wait, I can just write a script that takes the HTML and converts it. 
// But I don't have the user prompt directly in a file. I can extract it from the agent's memory?
// Actually I have it. I'll just write it.
